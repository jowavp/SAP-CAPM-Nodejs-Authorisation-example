ID: CAPMAuthorisation
_schema-version: "2.1"
description: Test Capm Authorisations
version: 0.0.1
modules:
  - name: CAPMAuthorisation-db
    type: hdb
    path: db
    parameters:
      memory: 256M
      disk-quota: 256M
    requires:
      - name: CAPMAuthorisation-db-hdi-container
  - name: CAPMAuthorisation-srv
    type: nodejs
    path: srv
    parameters:
      memory: 512M
      disk-quota: 256M
    provides:
      - name: srv_api
        properties:
          url: ${default-url}
    requires:
      - name: CAPMAuthorisation-db-hdi-container
      - name: CAPMAuthorisation-uaa
resources:
  - name: CAPMAuthorisation-db-hdi-container
    type: com.sap.xs.hdi-container
    properties:
      hdi-container-name: ${service-name}
  - name: CAPMAuthorisation-uaa
    type: org.cloudfoundry.managed-service
    parameters:
      service-plan: default
      service: xsuaa
      config:
        xsappname: CAPMAuthorisation-${space}
        tenant-mode: dedicated
